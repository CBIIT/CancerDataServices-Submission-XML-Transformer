from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Transform Submission XML to CDS Metadata Manifest format',
    author='bensonml',
    license='MIT',
)
